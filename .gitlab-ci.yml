stages:
  - deploy

pages:
  stage: deploy
  image: rocker/r-ver:4.2
  before_script:
    - apt-get update && apt-get install libpq5 libpq-dev -y  
    - Rscript -e 'install.packages(c("rvest","RSelenium","RPostgreSQL"))'

  script:   
    - Rscript jobstreet-web-scraping.R 
